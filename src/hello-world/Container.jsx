export default function Container({ children }) {
    return (
        <div>
            <h1>Belajar  ReactJS</h1>
            {children}
            <footer>
                <p>Brian sedang memplejari ReactJs</p>
            </footer>
        </div>
    )
}